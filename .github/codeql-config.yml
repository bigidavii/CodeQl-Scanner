name: "DOM XSS Focused CodeQL Configuration"

# This configuration uses the 'security-and-quality' query suite.
# This is a comprehensive set of queries for finding security vulnerabilities
# and maintaining code quality. It includes robust checks for DOM-based
# Cross-Site Scripting (XSS), which corresponds to CWE-079.
#
# By using this suite, we are running a powerful analysis that is well-suited
# for finding the types of vulnerabilities you are concerned about.
queries:
  - uses: security-and-quality
